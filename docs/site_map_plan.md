현재 MVP 화면은 **6개 라우트 + 전역 챗봇 위젯 1개**로 구성합니다. 회원가입·로그인은 없으며, 게시글 CRUD는 비밀번호 확인 방식으로 처리합니다. 장소 지도·검색·목록은 하나의 통합 지도 화면에서 제공합니다.

## 1. 전체 정보 구조

```text
LocalHub
├─ 홈
│  ├─ 선정 권역 소개
│  ├─ 지역 지도 바로가기
│  └─ 커뮤니티 바로가기
│
├─ 지역 지도
│  ├─ 7개 콘텐츠 유형 필터
│  ├─ 장소 마커
│  └─ 장소 기본정보 팝업
│
├─ 커뮤니티
│  ├─ 게시글 목록
│  ├─ 게시글 상세
│  ├─ 게시글 작성
│  └─ 게시글 수정
│
└─ 챗봇 위젯
   ├─ 지역 정보 질문
   ├─ 커뮤니티 게시글 검색 질문
   └─ 대화 히스토리
```

## 2. 라우트 구조

| 화면 ID | 화면명    | 경로                | 핵심 목적                |
| ----- | ------ | ----------------- | -------------------- |
| H-01  | 홈      | `/`               | 서비스 소개와 지도·커뮤니티 진입   |
| M-01  | 지역 지도  | `/map`            | 제공 관광 데이터의 위치 확인     |
| B-01  | 게시글 목록 | `/posts`          | 익명 게시글 목록 조회         |
| B-02  | 게시글 상세 | `/posts/:id`      | 게시글 내용 조회 및 수정·삭제 진입 |
| B-03  | 게시글 작성 | `/posts/new`      | 새 게시글 등록             |
| B-04  | 게시글 수정 | `/posts/:id/edit` | 기존 내용을 조회하고 비밀번호 확인 후 수정 |
| C-01  | 챗봇 위젯  | 전 화면 공통           | 지역 정보 및 게시글 자연어 질의   |

작성 화면과 수정 화면은 라우트는 나누되, 실제 구현에서는 하나의 `PostForm` 컴포넌트를 재사용합니다.

---

## 3. 공통 레이아웃

### 데스크톱

```text
┌──────────────────────────────────────────────────────┐
│ LocalHub              홈   지역 지도   커뮤니티       │
├──────────────────────────────────────────────────────┤
│                                                      │
│                    현재 화면 콘텐츠                   │
│                                                      │
│                                               [챗봇] │
├──────────────────────────────────────────────────────┤
│ 데이터 출처 / 프로젝트 안내                           │
└──────────────────────────────────────────────────────┘
```

### 모바일

```text
┌──────────────────────────┐
│ LocalHub            [☰]  │
├──────────────────────────┤
│                          │
│      현재 화면 콘텐츠     │
│                          │
│                   [챗봇] │
└──────────────────────────┘
```

모바일 메뉴는 `홈 / 지역 지도 / 커뮤니티` 3개를 제공합니다. 챗봇을 열면 모바일에서는 전체 화면 대화창으로 전환합니다.

---

# 4. 화면별 상세 설계

## H-01. 홈 화면

### 목적

사용자가 서비스 성격을 즉시 이해하고 지도 또는 커뮤니티로 이동하게 합니다.

### 화면 구성

```text
┌──────────────────────────────────────────────┐
│ [선정 권역명] 지역 정보 공유 커뮤니티          │
│ 관광 정보와 주민들의 이야기를 확인해 보세요.   │
│                                              │
│ [지역 지도 보기]      [커뮤니티 보기]          │
├──────────────────────────────────────────────┤
│ 서비스 안내                                  │
│ · 7개 유형 관광 정보 제공                     │
│ · 익명 게시판 운영                            │
│ · 지역 정보 챗봇 제공                         │
└──────────────────────────────────────────────┘
```

### 주요 동작

| 사용자 행동      | 결과           |
| ----------- | ------------ |
| 지역 지도 보기 클릭 | `/map`으로 이동  |
| 커뮤니티 보기 클릭  | `/posts`로 이동 |
| 챗봇 버튼 클릭    | 챗봇 대화창 열기    |

최근 게시글 목록이나 관광지 추천 카드는 Must 범위에서 제외하여 홈을 단순하게 유지합니다.

---

## M-01. 지역 지도 화면

지도 API는 장소명, 콘텐츠 유형, 주소, `latitude`, `longitude`를 경량 응답으로 제공합니다. 프런트는 Leaflet의 현재 화면 경계를 `south`, `west`, `north`, `east`로 전달해 보이는 영역의 장소만 조회합니다.

### 화면 구성

```text
┌────────────────────────────────────────────────────┐
│ 지역 지도                                           │
│ [전체] [관광지] [문화시설] [축제] [여행코스]         │
│ [레포츠] [숙박] [쇼핑]                               │
├────────────────────────────────────────────────────┤
│                                                    │
│                    Leaflet 지도                     │
│         ●      ●                    ●              │
│                 ●                                  │
│                                                    │
└────────────────────────────────────────────────────┘
```

### 마커 팝업 최소 정보

```text
┌────────────────────────┐
│ 장소명                  │
│ 주소                    │
└────────────────────────┘
```

전화번호와 대표 이미지는 기존 MoSCoW에서 Should로 분류했으므로 Must-only 구현에서는 제외할 수 있습니다.

### 주요 동작

| 사용자 행동    | 결과                  |
| --------- | ------------------- |
| 화면 진입     | 전체 유형의 유효한 장소 마커 표시 |
| 콘텐츠 유형 클릭 | 해당 유형의 선택 상태 토글, 복수 유형 동시 표시 |
| 마커 클릭     | 장소명과 주소 팝업 표시       |
| 전체 클릭     | 전체 선택 상태면 모두 해제, 일부·미선택 상태면 모두 선택 |

### 데이터 예외 처리

| 데이터 상태              | 화면 처리                  |
| ------------------- | ---------------------- |
| `latitude` 또는 `longitude` 없음 | 지도 마커에서 제외             |
| 좌표 숫자 변환 실패         | 지도 마커에서 제외             |
| `addr1` 빈 문자열       | `주소 정보 없음` 표시          |
| API 로딩 중            | 지도 중앙에 로딩 표시           |
| API 실패              | `장소 정보를 불러오지 못했습니다` 표시 |
| 표시 가능한 장소 없음        | `표시할 장소가 없습니다` 표시      |

---

### 지도·목록 통합 탐색

지도 옆 목록에서 현재 조회 영역의 장소를 검색하고 비교합니다. 별도 장소 목록 라우트는 만들지 않으며 지도와 목록은 같은 `MarkerPlace[]` 상태를 공유합니다.

#### 데스크톱

```text
┌──────────────────┬──────────────────────────────────┐
│ [장소명·주소 검색] │                                  │
│ [전체][관광지]...  │                                  │
│ 현재 영역 123개    │              지도                │
│ 장소 목록          │                                  │
│ 장소 목록          │       [이 지역에서 다시 검색]     │
│ [이전] 1/4 [다음]  │                                  │
└──────────────────┴──────────────────────────────────┘
```

- 1024px 초과에서는 약 390px, 761~1024px에서는 약 320px 사이드바를 사용합니다.
- 목록 선택 시 해당 마커로 지도를 이동하고 선택 상태를 강조합니다.
- 지도 사이드바와 마커 팝업에는 장소 썸네일을 표시하며, 이미지가 없거나 실패하면 카테고리별 기본 SVG를 사용합니다.
- 마커 선택 시 마커 위에 썸네일·카테고리·장소명·주소 팝업을 표시하고, 목록의 같은 항목을 강조해 보이는 위치로 스크롤합니다.
- 선택 장소 정보를 사이드바 상단에 별도로 삽입하지 않아 기존 목록 위치가 밀리지 않게 합니다.
- 지도 이동만으로 결과를 즉시 바꾸지 않고 `[이 지역에서 다시 검색]` 버튼을 표시합니다.

#### 모바일

목록 사이드바는 지도 하단의 버튼식 바텀 시트로 전환합니다. 드래그 제스처는 MVP에서 제외합니다.

| 단계 | 높이와 내용 |
| --- | --- |
| 축소 | 약 82px, 현재 영역 장소 수와 `[목록 보기]` 표시 |
| 중간 | 화면의 약 48%, 검색·필터와 일부 목록 표시 |
| 전체 | 화면의 약 90%, 검색·필터·전체 목록 탐색 |

시트 상단 버튼을 누를 때 `축소 → 중간 → 전체 → 축소` 순서로 전환합니다. 마커를 누르면 자동으로 중간 상태를 열어 선택한 장소 정보를 보여줍니다.

#### 검색·필터 규칙

- 검색은 현재 `/api/places/map`으로 받은 영역 데이터의 `name`, `address`를 대상으로 합니다.
- 입력할 때마다 검색하지 않고 Enter 또는 `[검색]`을 눌렀을 때 적용합니다.
- 카테고리는 복수 선택하며, 전체 선택 상태에서는 카테고리 없는 API를 한 번 호출합니다.
- 선택 카테고리가 없으면 API를 호출하지 않고 마커와 목록을 비웁니다.
- 검색 또는 카테고리 변경 시 목록 페이지를 1로 초기화합니다.
- 목록은 한 페이지에 12개를 표시합니다.
- 지도 영역 응답이 `truncated: true`이면 확대 안내를 함께 표시합니다.

#### 상태와 접근성

| 상태 | UI 처리 |
| --- | --- |
| 최초·영역 재조회 | 사이드바 목록에 로딩 표시 |
| 검색 결과 없음 | `조건에 맞는 장소가 없습니다`와 검색 초기화 제공 |
| 카테고리 미선택 | `표시할 장소 유형을 선택해 주세요` 표시 |
| API 실패 | 오류 문구와 `[다시 시도]` 제공 |
| 지도 이동 후 | 기존 결과를 유지하고 `[이 지역에서 다시 검색]` 표시 |

검색창과 목록 버튼에는 접근 가능한 이름을 제공하고, 장소 항목은 키보드 포커스와 선택 강조를 지원합니다.

---

## B-01. 게시글 목록 화면

### 화면 구성

```text
┌──────────────────────────────────────────────────┐
│ 커뮤니티                              [글쓰기]    │
├──────┬───────────────────────────────┬───────────┤
│ 번호 │ 제목                          │ 작성일     │
├──────┼───────────────────────────────┼───────────┤
│ 10   │ 여행 코스 추천 부탁드립니다    │ 07/14     │
│  9   │ 문화시설 방문 후기             │ 07/14     │
│  8   │ 숙소 주변 관광지 추천          │ 07/13     │
└──────┴───────────────────────────────┴───────────┘
│               [이전] 1 2 3 [다음]                │
└──────────────────────────────────────────────────┘
```

### 주요 동작

| 사용자 행동    | 결과               |
| --------- | ---------------- |
| 화면 진입     | 최신순 게시글 목록 조회    |
| 게시글 제목 클릭 | `/posts/:id`로 이동 |
| 글쓰기 클릭    | `/posts/new`로 이동 |
| 페이지 번호 클릭 | `/posts?page={page}`로 이동 후 해당 페이지 조회 |

페이지 상태는 URL 쿼리스트링에 보존합니다. 상세 화면에서 목록으로 돌아올 때도 이전 `page` 값을 유지하며, API 요청의 `size`는 MVP에서 `10`으로 고정합니다. 유효하지 않은 페이지 값은 `1`로 보정합니다.

게시글 API는 향후 확장을 위해 `keyword` 검색을 지원하지만, 검색은 Should 범위이므로 MVP 목록 화면에는 검색창을 노출하지 않습니다. 챗봇의 게시글 검색 질의는 별도로 지원합니다.

### 화면 상태

* 로딩: 목록 대신 스켈레톤 또는 `불러오는 중`
* 빈 목록: `아직 등록된 게시글이 없습니다`와 `[글쓰기]` 버튼 표시
* 페이지 범위 초과: 빈 목록을 표시하고 첫 페이지로 이동할 수 있는 버튼 제공
* 오류: 오류 메시지와 재시도 버튼 표시

데스크톱에서는 번호·제목·작성일 테이블을 사용합니다. 모바일에서는 번호를 숨기고 제목과 작성일을 세로로 배치한 카드형 목록으로 전환합니다.

---

## B-02. 게시글 상세 화면

### 화면 구성

```text
┌─────────────────────────────────────────────────┐
│ 게시글 제목                                      │
│ 작성일: 2026.07.14                               │
├─────────────────────────────────────────────────┤
│                                                 │
│ 게시글 본문                                      │
│                                                 │
├─────────────────────────────────────────────────┤
│ 여행 일정 타임라인                               │
│ [1] 장소 A → [2] 장소 B                          │
│                                                 │
│ 여행 일정 지도                                   │
│        ● 1 ┈┈┈ ● 2                              │
├─────────────────────────────────────────────────┤
│ [목록]                         [수정] [삭제]      │
└─────────────────────────────────────────────────┘
```

### 주요 동작

| 사용자 행동       | 결과                |
| ------------ | ----------------- |
| 목록 클릭        | 이전 페이지 쿼리를 유지한 `/posts?page={page}`로 이동 |
| 수정 클릭        | `/posts/:id/edit`로 바로 이동 |
| 삭제 클릭        | 삭제용 비밀번호 확인 모달 표시 |
| 존재하지 않는 글 진입 | 404 안내 후 목록 버튼 표시 |
| 일정 지도 마커 클릭 | Day·순서·장소명·시간·메모 팝업 표시 |

여행 일정에 좌표가 있는 장소가 하나 이상 있을 때만 일정 지도를 표시합니다. 날짜별로 마커와 연결선 색을 구분하며 좌표가 없는 장소는 타임라인에는 유지하고 지도에서만 제외합니다.

### 삭제용 비밀번호 확인 모달

```text
┌────────────────────────────┐
│ 게시글 삭제                  │
│ [비밀번호 입력           ]  │
│ 비밀번호가 일치하면 게시글이 │
│ 즉시 삭제됩니다.             │
│                            │
│ [취소]             [삭제]  │
└────────────────────────────┘
```

#### 삭제 흐름

```text
삭제 클릭
→ 비밀번호 입력
→ 서버 검증 및 삭제
→ 성공: 목록 화면 이동
→ 비밀번호 불일치: 모달 유지, 비밀번호만 비우고 오류 메시지 표시
```

삭제 요청 중에는 취소·삭제 버튼을 비활성화하여 중복 요청을 방지합니다. 별도의 삭제 확인창은 추가하지 않습니다.

---

## B-03. 게시글 작성 화면

### 화면 구성

```text
┌─────────────────────────────────────────────────┐
│ 게시글 작성                                      │
├─────────────────────────────────────────────────┤
│ 제목                                             │
│ [                                             ]  │
│                                                 │
│ 내용                                             │
│ [                                             ]  │
│ [                                             ]  │
│                                                 │
│ 여행 일정 편집                                    │
│ DAY 1  [장소 A] [장소 B]                         │
│                                                 │
│ 일정 지도 미리보기                                │
│        ● 1 ┈┈┈ ● 2                              │
│                                                 │
│ 수정용 비밀번호                                  │
│ [                    ]                           │
│ 게시글 수정·삭제 시 필요합니다.                   │
│                                                 │
│                           [취소] [등록]           │
└─────────────────────────────────────────────────┘
```

### 입력 검증

| 필드       | 검증            |
| -------- | ------------- |
| 제목       | 앞뒤 공백 제거 후 1~100자 |
| 내용       | 앞뒤 공백 제거 후 1~5000자 |
| 수정용 비밀번호 | 4~20자, 공백만 입력 불가 |

제목과 내용은 프런트에서도 공백을 정규화하지만, 비밀번호는 앞뒤 공백을 자동 제거하지 않습니다. 백엔드 검증을 최종 기준으로 하며 `422` 응답의 필드별 오류를 해당 입력란 아래에 표시합니다.

일정 편집에서 좌표가 있는 장소가 추가·삭제되거나 순서가 변경되면 같은 `ScheduleMap` 컴포넌트가 지도 미리보기를 즉시 갱신합니다. 메모·시간 변경은 팝업 내용만 갱신하고 지도 중심을 불필요하게 재설정하지 않습니다.

### 사용자 흐름

```text
게시글 목록
→ 글쓰기
→ 제목·내용·비밀번호 입력
→ 등록
→ 성공: 작성된 게시글 상세 화면
→ 실패: 입력값 유지 + 오류 메시지
```

등록 요청 중에는 등록 버튼을 비활성화합니다. 취소하면 게시글 목록으로 이동합니다.

---

## B-04. 게시글 수정 화면

게시글 작성 화면과 동일한 `PostForm` 컴포넌트를 사용합니다.

### 차이점

| 항목      | 작성 화면    | 수정 화면            |
| ------- | -------- | ---------------- |
| 제목      | 빈 값      | 기존 제목            |
| 내용      | 빈 값      | 기존 내용            |
| 비밀번호    | 폼에서 신규 입력 | 수정 제출 시 모달에서 입력 |
| 실행 버튼   | 등록       | 수정 완료            |
| 성공 후 이동 | 새 게시글 상세 | 기존 게시글 상세        |
| 실패 시 | 입력값 유지 | 수정 내용 유지 |

수정 화면에 직접 접근할 수 있으므로 진입 시 `GET /api/posts/{id}`로 기존 제목과 내용을 조회합니다. 조회 중에는 폼을 비활성화하고, 404이면 게시글 목록 이동 버튼을, 서버 오류이면 재시도 버튼을 표시합니다.

```text
수정 화면 진입
→ 기존 게시글 조회
→ 제목·내용 수정
→ 수정 완료 클릭
→ 비밀번호 모달 표시
→ 비밀번호와 수정 내용을 함께 서버로 전송
→ 성공: 게시글 상세로 이동
```

별도 비밀번호 검증 API는 사용하지 않습니다. `403 INVALID_PASSWORD`이면 수정 내용과 모달을 유지하고 비밀번호 입력값만 비운 뒤 해당 필드에 포커스를 이동합니다. 요청 중에는 확인 버튼을 비활성화합니다. 비밀번호는 URL, 라우터 상태, Pinia, `localStorage`에 저장하지 않습니다. 취소하면 해당 게시글 상세 화면으로 돌아갑니다.

---

## C-01. 챗봇 위젯

챗봇은 특정 페이지가 아니라 모든 화면에 표시되는 전역 컴포넌트입니다. RFP는 제공 JSON 기반 질의응답, 게시글 검색 등의 주요 질의 대응과 플로팅·대화 히스토리·모바일 대응 UI를 요구합니다. 

### 접힌 상태

```text
화면 우측 하단
        ┌──────┐
        │ 챗봇 │
        └──────┘
```

### 데스크톱 펼친 상태

```text
┌─────────────────────────────┐
│ LocalHub 챗봇          [×]  │
├─────────────────────────────┤
│ 안녕하세요. 지역 정보를     │
│ 물어보세요.                  │
│                             │
│          관광지 추천해 줘    │
│                             │
│ 추천 장소는 다음과 같습니다 │
├─────────────────────────────┤
│ [메시지를 입력하세요] [전송] │
└─────────────────────────────┘
```

### 모바일 펼친 상태

```text
┌──────────────────────────┐
│ LocalHub 챗봇       [×]  │
├──────────────────────────┤
│                          │
│       전체 대화 영역      │
│                          │
├──────────────────────────┤
│ [메시지 입력]     [전송] │
└──────────────────────────┘
```

### 지원 질문 범위

| 질문 유형  | 예시               | 처리 데이터      |
| ------ | ---------------- | ----------- |
| 관광 정보  | `추천 관광지를 알려줘`    | 관광지 JSON    |
| 문화시설   | `주변 문화시설이 있어?`   | 문화시설 JSON   |
| 숙박     | `숙박 시설을 알려줘`     | 숙박 JSON     |
| 레포츠    | `레포츠 장소 추천해 줘`   | 레포츠 JSON    |
| 쇼핑     | `쇼핑할 곳을 알려줘`     | 쇼핑 JSON     |
| 여행코스   | `여행 코스를 알려줘`     | 여행코스 JSON   |
| 축제 정보  | `축제 장소를 알려줘`     | 축제공연행사 JSON |
| 게시글 검색 | `숙박에 관한 게시글 찾아줘` | SQLite 게시글  |

축제 데이터에는 실제 행사 시작일과 종료일이 없으므로, 챗봇이 축제 일정을 묻는 질문에는 날짜를 임의 생성하지 않습니다.

```text
제공 데이터에 행사 날짜 정보가 없어 정확한 일정을 안내할 수 없습니다.
축제 장소 정보는 안내할 수 있습니다.
```

### 대화 상태

* 대화 히스토리는 현재 브라우저 세션 동안 유지
* 페이지 이동 후에도 유지하려면 Pinia 또는 상위 `App.vue` 상태 사용
* 새로고침 후 보존은 Must 범위에서 제외
* API 요청 중 입력창과 전송 버튼 비활성화
* 답변 실패 시 `답변을 불러오지 못했습니다. 다시 시도해 주세요.` 표시

---

# 5. 전체 사용자 흐름

## 흐름 A. 관광 정보 탐색

```text
홈
→ 지역 지도 보기
→ 콘텐츠 유형 선택 또는 현재 영역 장소 검색
→ 지도 마커·사이드바 목록 확인
→ 마커 또는 목록 항목 선택
→ 장소명·주소 확인
→ 지도 이동
→ 이 지역에서 다시 검색
```

## 흐름 B. 게시글 조회

```text
홈
→ 커뮤니티 보기
→ 게시글 목록
→ 게시글 선택
→ 게시글 상세
→ 목록으로 돌아가기
```

## 흐름 C. 게시글 작성

```text
게시글 목록
→ 글쓰기
→ 제목·내용·비밀번호 입력
→ 등록
→ 게시글 상세
```

## 흐름 D. 게시글 수정

```text
게시글 상세
→ 수정
→ 수정 화면
→ 내용 변경
→ 수정 완료
→ 비밀번호 입력
→ 비밀번호 일치
→ 게시글 상세
```

실패 흐름:

```text
비밀번호 불일치
→ 수정 화면 유지
→ "비밀번호가 일치하지 않습니다" 표시
```

## 흐름 E. 게시글 삭제

```text
게시글 상세
→ 삭제
→ 비밀번호 입력
→ 비밀번호 일치
→ 게시글 삭제
→ 게시글 목록
```

실패 흐름:

```text
비밀번호 불일치
→ 상세 화면 유지
→ 오류 메시지 표시
```

## 흐름 F. 챗봇 이용

```text
임의의 화면
→ 챗봇 플로팅 버튼 클릭
→ 질문 입력
→ POST /api/chat
→ 답변 표시
→ 후속 질문
→ 챗봇 닫기
→ 기존 화면 계속 이용
```

---

# 6. 화면과 API 연결

| 화면     | API                                  | 사용 목적        |
| ------ | ------------------------------------ | ------------ |
| 지역 지도  | `GET /api/places/map?south=&west=&north=&east=` | 현재 지도 영역의 경량 마커 조회 |
| 지역 지도  | `GET /api/places/map?...&category=관광지` | 유형별 지도 마커 조회 |
| 지역 지도 | `GET /api/places/categories` | 콘텐츠 유형 필터 구성 |
| 지역 지도 | `GET /api/places/{place_id}` | `/map?place=` 진입 시 선택 장소 위치 확인 |
| 게시글 목록 | `GET /api/posts?page=1&size=10`      | 게시글 목록       |
| 게시글 상세 | `GET /api/posts/{id}`                | 게시글 상세       |
| 게시글 작성 | `POST /api/posts`                    | 게시글 등록       |
| 게시글 수정 | `PUT /api/posts/{id}`                | 비밀번호 확인 및 수정 |
| 게시글 삭제 | `DELETE /api/posts/{id}`             | 비밀번호 확인 및 삭제 |
| 챗봇     | `POST /api/chat`                     | 지역·게시글 질의응답  |

수정·삭제 요청 예시는 다음처럼 통일하는 것이 단순합니다.

```json
{
  "password": "1234"
}
```

수정 요청은 제목과 내용을 함께 전달합니다.

```json
{
  "title": "수정된 제목",
  "content": "수정된 내용",
  "password": "1234"
}
```

---

# 7. 공통 화면 상태 규칙

| 상태       | UI 처리                        |
| -------- | ---------------------------- |
| 로딩       | 콘텐츠 영역에 로딩 표시하고 관련 입력·실행 버튼 비활성화 |
| 게시글 목록 없음 | 안내 문구와 `[글쓰기]` 버튼 표시 |
| 지도 조회 결과 없음 | `표시할 장소가 없습니다` 표시 |
| 지도 목록 검색 결과 없음 | 검색 조건 유지, 안내 문구와 `[검색 초기화]` 표시 |
| 입력 오류    | 해당 필드 아래 오류 문구, 입력값 유지 |
| 서버 오류    | 화면 상단 또는 토스트로 오류 안내하고 재시도 제공 |
| 게시글 404 | `요청한 게시글을 찾을 수 없습니다`와 목록 버튼 |
| 삭제 성공    | 게시글 목록으로 이동 후 완료 메시지         |
| 작성·수정 성공 | 해당 게시글 상세 화면으로 이동            |
| 변경 요청 중 | 실행 버튼 비활성화로 중복 요청 방지 |
| 챗봇 요청 중  | 전송 버튼 비활성화 및 응답 대기 표시        |

## 최종 화면 범위

현재 MVP 기준으로 구현하는 화면은 다음과 같습니다.

> **홈 1개 + 지도·목록 통합 화면 1개 + 게시판 4개 + 전역 챗봇 1개**

장소 지도·목록·검색은 `/map`에 통합합니다. 이후 고급 정렬·상세 필터·검색엔진 노출이 필요해지면 `/places`와 `/places/:place_id`를 분리할 수 있지만, MVP에서는 지도와 목록의 양방향 연동 및 모바일 바텀 시트를 우선합니다. 회원·관리자 화면은 현재 범위에서 제외합니다.
